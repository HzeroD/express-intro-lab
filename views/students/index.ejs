<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    

    <%students.forEach(function(student){ %>
            
            <% if(student.minor) {%>
            <p>
                <%= `Get ${student.name} outta here! He's a minor!`%>
            </p><br>
            <% } %><% if (!student.minor) { %>
                <p>
            <%=`Get ${student.name} some vodka!`%>
                <p>
           <% } %>
            
            <%})%>

    
    
</body>
</html>